// src/theme.ts
import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  palette: {
    background: {
      default: '#F1F4F9', // Your custom background color
    },
  },
});

export default Theme;
